spec:
  inputs:
    job-stage:
      default: .post
---
GitTerra:
  stage: $[[ inputs.job-stage ]]
  script:
    - echo "Let's play GitTerra!"
    - mkdir maps
    - touch maps/index.html
  artifacts:
    paths:
      - maps/
    expire_in: 1 week
